<%= form_for(@page) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% @page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
 	<%= f.text_field :name, placeholder: "Name", class:"formInput" %>
	<%= f.text_field :lastname, placeholder: "Last Name", class:"formInput" %>
	<%= f.text_field :email, placeholder: "Email address", class:"formInput" %>

  <%= f.submit  "Notify me", class: "register_button"%>
  
<% end %>
